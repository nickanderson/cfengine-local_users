{
  "name": "Inventory local users cfbs module",
  "description": "Inventory the local users on the system with their attributes",
  "type": "module",
  "provides": {
    "inventory-local-users": {
      "name": "inventory-local-users",
      "description": "Inventory the local users on the system with their attributes",
      "tags": ["untested"],
      "repo": "https://github.com/nickanderson/cfengine-local_users",
      "version": "2.0.0",
      "steps": [
        "copy ./policy/parsed_etc_passwd_shadow.cf services/inventory-local-users/",
        "json cfbs/inventory-local-users.def.json def.json"
      ]
    }
  }
}
