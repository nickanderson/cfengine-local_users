{
    "name": "inventory-local-users",
    "description": "Enumerate the local users on the system with their attributes",
    "tags": ["wip", "untested"],
    "version": "1.0.0",
    "steps": ["copy ./policy/inventory_passwd_shadow_discover.cf services/inventory-local-users/",
              "json cfbs/def.json def.json"
             ]
}
